export default {
    server: {
		mode: 'production',
        devServer: {},
        plugins: [

        ],
        rules: [

        ]
    },
    client: {
		mode: 'production',
        devServer: {},
        plugins: [

        ],
        rules: [

        ]
    }
};